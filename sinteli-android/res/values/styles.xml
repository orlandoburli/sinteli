<resources>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light"></style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:textColor">@color/cor_texto_app</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>